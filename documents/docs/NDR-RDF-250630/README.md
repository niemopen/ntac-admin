This folder will be replaced by a single .pdf when submitted.  Until then it contains both markdown and image files
used to generate the output, to avoid polluting the docs folder with multiple files.